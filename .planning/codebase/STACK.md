# Technology Stack

> Generated by gsd-codebase-mapper | Focus: tech | Date: 2026-03-24

## Languages

**Primary:**
- Dart ^3.8.1 - All application code (`lib/`, `test/`)

**Secondary:**
- TypeScript ^5.6.3 - Cloud Functions (NOT deployed; Spark plan limitation) (`functions/src/`)
- Kotlin - Android platform glue (`android/app/build.gradle.kts` uses `kotlin-android` plugin, Java 11 target)

## Runtime

**Environment:**
- Flutter SDK (latest stable) - Cross-platform UI framework
- Node.js 20 - Cloud Functions runtime (dormant; `functions/package.json` `engines.node: "20"`)
- Dart SDK ^3.8.1 - Specified in `pubspec.yaml` `environment.sdk`

**Package Manager:**
- pub (Dart/Flutter) - `pubspec.yaml` + `pubspec.lock` present
- npm - Cloud Functions only (`functions/package-lock.json` present)
- Lockfile: present for both pub and npm

## Frameworks

**Core:**
- Flutter (Material Design) - `pubspec.yaml` `uses-material-design: true`
- Firebase - Backend-as-a-Service (Firestore, Auth, Storage)

**Testing:**
- `flutter_test` (built-in) - Widget and unit testing
- `mockito` ^5.4.4 - Mock generation for unit tests
- `fake_cloud_firestore` ^3.1.0 - Firestore emulation in tests
- `firebase_auth_mocks` ^0.14.1 - Firebase Auth emulation in tests
- `jest` ^29.7.0 + `ts-jest` ^29.2.5 - Cloud Functions tests (TypeScript)

**Build/Dev:**
- `build_runner` ^2.4.13 - Code generation (mockito stubs)
- `flutter_lints` ^5.0.0 - Static analysis rules (`analysis_options.yaml` includes `package:flutter_lints/flutter.yaml`)
- Gradle (Kotlin DSL) - Android build (`android/app/build.gradle.kts`)
- `com.google.gms.google-services` - Firebase Android plugin

## Key Dependencies

**Firebase Suite:**
- `firebase_core` ^3.8.1 - Firebase initialization (`lib/main.dart` line 18)
- `cloud_firestore` ^5.6.0 - Primary database, offline persistence enabled (`lib/services/firebase_service.dart` lines 73-76)
- `firebase_auth` ^5.3.4 - Email/password + Google Sign-In authentication (`lib/services/firebase_service.dart` line 28)
- `firebase_storage` ^12.4.0 - Profile photo upload/delete at `users/{uid}/profile.jpg` (`lib/services/firebase_service.dart` lines 691-724)

**Networking:**
- `dio` ^5.3.0 - Primary HTTP client with retry interceptor; used for TheMealDB API calls and Cloud Vision REST API (`lib/services/firebase_service.dart` lines 47-70)
- `http` ^1.1.0 - Secondary HTTP client (declared as dependency but not observed in active use)

**State Management:**
- `provider` ^6.0.0 - ChangeNotifier-based state management; five providers registered in `lib/main.dart` lines 49-62

**Navigation:**
- `go_router` ^10.0.0 - Declarative routing with ShellRoute, auth redirect guard (`lib/app/routes.dart`)

**Authentication:**
- `google_sign_in` ^6.2.1 - Google OAuth flow (`lib/services/firebase_service.dart` lines 291-327)

**UI/UX:**
- `cached_network_image` ^3.3.0 - Network image caching (recipe thumbnails)
- `google_fonts` ^6.2.1 - Poppins font loaded at runtime (`lib/app/theme/app_typography.dart`)
- `cupertino_icons` ^1.0.2 - iOS-style icons

**Device Capabilities:**
- `image_picker` ^1.0.0 - Camera + gallery image capture for scan feature and profile photos
- `speech_to_text` ^7.3.0 - On-device voice recognition for voice search (`lib/services/voice_search_service.dart`)
- `permission_handler` ^11.4.0 - Runtime permission requests (camera, microphone)

**Storage & Persistence:**
- `shared_preferences` ^2.2.0 - Local key-value store for onboarding flag, theme, offline favorites/grocery cache (`lib/app/constants.dart` `PrefKeys`)

**Sharing:**
- `share_plus` ^7.2.0 - System share sheet for recipe sharing
- `url_launcher` ^6.3.0 - Opening external URLs (help, feedback email, Play Store)

## Dev Dependencies

- `flutter_test` (SDK) - Widget and unit test framework
- `flutter_lints` ^5.0.0 - Lint rules
- `mockito` ^5.4.4 - Mock generation
- `build_runner` ^2.4.13 - Code generation runner
- `fake_cloud_firestore` ^3.1.0 - In-memory Firestore for testing
- `firebase_auth_mocks` ^0.14.1 - Firebase Auth mocking

## Configuration

**Environment Variables:**
- `VISION_API_KEY` - Google Cloud Vision API key, passed via `--dart-define-from-file=dart_defines/dev.json` at build time (`lib/services/firebase_service.dart` lines 112-113)
- Template at `dart_defines/dev.json.example`; actual file is gitignored (`dart_defines/*.json` in `.gitignore`)

**Build Configuration:**
- `pubspec.yaml` - Dart/Flutter dependencies, assets declaration
- `analysis_options.yaml` - Linting; includes `package:flutter_lints/flutter.yaml`
- `android/app/build.gradle.kts` - Android build config (namespace `com.example.smartchefai`, NDK 28.2.13676358, Java 11)
- `firebase.json` - Firestore rules, Storage rules, Cloud Functions config
- `firestore.rules` - Security rules for 5 collections (recipes, users, grocery_lists, meal_plans, nutrition_goals)
- `storage.rules` - Storage security (5 MB image limit per user path)
- `.vscode/launch.json` - VS Code debug config with `--dart-define-from-file` flag
- `.vscode/settings.json` - Editor settings

**Assets:**
- `assets/images/` - Static image assets
- `assets/icons/` - Icon assets
- `data/recipes.json` - Local recipe data (4116 lines, ~100 recipes) for offline-first loading

## Platform Targets

**Configured and Active:**
- Android - `google-services.json` present, `firebase_options.dart` has real Android config (`lib/firebase_options.dart` lines 62-68)
- Web - `firebase_options.dart` has real Web config with `authDomain`, `measurementId` (`lib/firebase_options.dart` lines 50-58), `web/index.html` present

**Placeholder Only (NOT configured):**
- iOS - Placeholder API keys (`YOUR_IOS_API_KEY`) in `lib/firebase_options.dart` line 71
- macOS - Placeholder API keys in `lib/firebase_options.dart` line 79
- Windows - Placeholder API keys in `lib/firebase_options.dart` line 88
- Linux - Throws `UnsupportedError` at runtime (`lib/firebase_options.dart` line 36)

## Platform Requirements

**Development:**
- Flutter SDK (stable channel, Dart ^3.8.1)
- Android Studio or VS Code with Flutter/Dart extensions
- Google Cloud Vision API key in `dart_defines/dev.json`
- GCP project `smartchefai-344c5` with Vision API enabled

**Production:**
- Firebase Spark plan (free tier) - limits: no Cloud Functions deployment, standard Firestore/Auth/Storage quotas
- Cloud Vision API: 1,000 calls/month free (requires billing linked on GCP)
- Android: signed APK/AAB (currently using debug signing for release builds - `build.gradle.kts` line 40)
- Web: Firebase Hosting at `smartchefai.web.app` (`lib/app/constants.dart` line 22)

## App Metadata

- App name: SmartChef AI
- Version: 0.1.0 (pubspec) / 1.0.0 (display, `lib/app/constants.dart` line 31)
- Android package: `com.example.smartchefai`
- Firebase project: `smartchefai-344c5`

---

*Stack analysis: 2026-03-24*
